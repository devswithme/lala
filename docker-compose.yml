services:
  hono-api:
    build: .
    container_name: lala-api
    ports:
      - "3002:3002"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - nginx

networks:
  nginx:
    name: nginx
    external: true
